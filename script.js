const Gameboard = () => {
    const board = Array(9).fill(null);

    
    const updateBoard = (position, marker) => {
        board.forEach((cell) => {
            console.log(cell);
        });
    }
    
}

// const game = Gameboard();
// game;

